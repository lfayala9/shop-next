import Layout from "@containers/Layout";

const Checkout = () => {
  return (
    <Layout authPage={true}>
      <div>
        <h1>Checkout</h1>
      </div>
    </Layout>
  );
};

export default Checkout;
